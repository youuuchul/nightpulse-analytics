# 진행 상태 (2026-09-23, 백로그 §1~§4 대시보드 작업)

- 완료: 퍼널 탭(`src/tabs/FunnelTab.tsx`, 탐색 탭 대체) — 스코어보드 4(전체 전환율·최대 이탈 단계·직전 대비 가장 크게 변한 단계·탐색 도달률) → 단계 전환율 추이(28일 이상 주별, 미만 일별, 1일은 단계 막대) + 세그먼트별 퍼널 히트맵 표 → 오디언스별 퍼널 → 경로 탐색 생키 → 요일×시간대.
- 완료: 공간별 보기를 행사·결제 탭으로 이동(`src/tabs/Venues.tsx`), 소개 페이지 `?page=about`(`src/About.tsx`, 헤더 '소개'/'대시보드' 왕복 시 URL 상태 유지, pushState로 뒤로 가기 가능).
- 신규 컴포넌트: `components/ReachTable.tsx`(열 단위 명도 히트맵 표), `components/PathSankey.tsx`(자체 SVG 생키, 링크 hover 툴팁·클릭 고정).
- 드릴다운 기간 규칙: 주간 마트 2종은 `week_start ∈ [mondayOf(기간 시작), 기간 끝]` 주를 합산(단위 사람·주 / 세션). 두 키가 없으면 카드에 '데이터 없음'.
- 시작 화면 선택은 뺐다 — 실마트 `weekly_path`에 landing_screen 열이 없어 2단계 이후를 정확히 거를 수 없음. 넣으려면 마트에 landing_screen 축 추가가 선행.
- 샘플 생성기에 두 키 추가(계약과 같은 열, C6·C7 합계 일치 확인). `public/data.json`은 실데이터 — 샘플은 `--out /tmp/...`로만 쓸 것.
- 캡처: `npm run capture -- --all` 에 퍼널(1일·1년 유료 iOS·7일 회원)·드릴다운 카드 2·데이터 없음·행사 공간별·소개 라이트/다크/폰 추가. 실데이터로 확인 완료, 폰 퍼널 헤더 줄바꿈·다크 이탈 노드 색·폰 데이터 흐름 도식 잘림·화살표 marker id 충돌은 수정 후 마지막 1회 캡처만 미확인.
- 남은 것: `docs/dashboard.md` 갱신(탭 표의 탐색 행 → 퍼널, 행사·결제 공간별 행, 계약 표에 두 키, 소개 페이지 절), `README.md` 구조 목록 갱신, 옛 캡처(02_explore·07_explore_venue·12_explore_*) 정리, 최종 캡처 육안 확인.
- 주의: 옛 URL `tab=explore`는 퍼널 탭(view=venue면 행사·결제 공간별)으로 매핑된다. extract.py는 수정하지 않았다(두 키 추출 경로는 상위 세션 확인 필요).
